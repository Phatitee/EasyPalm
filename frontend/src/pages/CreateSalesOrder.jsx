// frontend/src/pages/CreateSalesOrder.jsx
import React, { useState, useEffect } from 'react';
import axios from 'axios';

const CreateSalesOrder = () => {
    // ... โค้ดสำหรับหน้าขายสินค้า (จะคล้ายกับหน้า PurchaseProduct) ...
    // ตอนนี้ใส่แค่ข้อความไว้ก่อน
    return (
        <div>
            <h1 className="text-2xl font-bold mb-4">สร้างรายการขายสินค้า</h1>
            <p className="p-4 bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700">
                หน้านี้ยังไม่ได้สร้างฟังก์ชันการทำงาน แต่โครงสร้างหน้าและเมนูพร้อมแล้ว!
            </p>
        </div>
    );
};

export default CreateSalesOrder;